# general
- When adding new cross-platform hosts (macOS, Ubuntu Linux), maximize desktop experience parity with existing NixOS hosts (zbook's Sway+DMS+Kitty+Starship+Zsh setup) to the extent sensible given platform constraints — prefer equivalent tools (Aerospace over Mac native tiling, DMS on all platforms, same shell/terminal/prompt/config) rather than diverging to platform-native defaults. Confidence: 0.65
- Before implementing protocol-level or ecosystem features (clipboard sync, display protocols, etc.), research existing implementations in the NixOS/Wayland ecosystem first — search GitHub with `gh` CLI and the web to check if an existing NixOS service, packaged tool, or established pattern already covers the use case. Confidence: 0.85
- When researching existing implementations, prioritize well-known, established repos (popular dotfiles, official NixOS modules, canonical guides like the Wayland Ricing Guide) over obscure or unverified sources — this ensures findings are grounded in trustworthy, maintained code. Confidence: 0.85
- Prefer precise, targeted fixes over broad workarounds — identify exactly which UI layer is broken (e.g., native menu bar via GTK vs. web content via Chromium's blink renderer) and fix only that layer. Don't apply workarounds for symptoms that don't exist. Confidence: 0.75
- When writing or updating code/configuration, include comments explaining what happens, why, and when — cite sources (authoritative guides, canonical implementations, or prior research) inline. The user explicitly requested this for the clipboard sync plan and expects it as a general practice, not a one-off. Confidence: 0.85
- When fixing Electron app theming issues (dark theme not applying to menu bars, title bars, etc.), prefer a single global user session environment variable (e.g., `GTK_THEME=Adwaita:dark`) over patching each Electron app's wrapper script individually via Nixpkgs overlays/overrideAttrs. The global env var covers all apps at once, requires zero per-app maintenance, and is simpler to reason about. Confidence: 0.95
- For non-trivial feature implementation (anything spanning multiple files, services, or with ecosystem research required), follow the research→adapt→implement loop: first search the NixOS/Wayland ecosystem with `gh search code` and the web for canonical references and existing implementations, then revise the plan with source citations and an explicit reference to the canonical implementation, then implement. The user expects iterative refinement ("Do some research" → "Continue the research in respectable repos" → "Adapt and correct the plan according to the findings") as a standard workflow, not a one-shot. The plan file at `~/.commandcode/plans/<descriptive-name>.md` should grow alongside the research, not just record the final state. Confidence: 0.85
- When debugging a failing new test, prefer iterative fixing over dropping the test entirely — the user explicitly said "Don't drop it completely" when offered to remove a KVM subtest mid-debug. Tests document intent and contract; losing them costs more than the time to debug. Narrow the test scope if the full assertion is too complex, but keep it as a contract on the core property. Confidence: 0.75
- When adding a new KVM test or protocol check that boots a NixOS VM, run it locally with `nix build path:.#checks.x86_64-linux.<check> --no-link` before relying on CI to validate it — KVM tests are slow (10+ minutes) and CI feedback is expensive, so local validation first is faster and avoids burning CI cycles. The user explicitly requested "Check the new test locally" when a new subtest was added. Confidence: 0.80
- When iterating on CI fixes in a worktree, use `git -c core.hooksPath=/dev/null commit …` to bypass the local pre-commit hooks (statix, deadnix, treefmt, shellcheck, gitleaks, markdownlint) — they can fight the iterative fix loop where the local state is intentionally mid-fix. The repo's CI pipeline runs the same checks, so skipping them locally during a fix cycle is safe. The user uses this pattern regularly. Confidence: 0.70
- When the user provides terse, decisive commands like "yes", "go", "Do it", "try again", or "continue", treat them as go-ahead signals without re-explaining plans or asking for confirmation — the user has already authorized the next step and re-asking wastes time. Reserve clarification for genuinely ambiguous situations, not for routine progress. Confidence: 0.80
- When research reveals that an existing implementation is the canonical reference (e.g., the Wayland Ricing Guide §125.6, the ArchWiki one-way recipe, a specific dotfile repo), reference it by name in the PR description and in inline code comments — the user explicitly requested "Make reference to that 'canonical implementation'" and wants the source to be a one-click confirmation for future maintainers. Confidence: 0.85
